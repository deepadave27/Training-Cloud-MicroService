spring:
  cloud:
    stream:
      bindings:
        helloinchannel:
          destination: helloexchange
          group: helloq
          binder: rabbit1
       
      binders:
        rabbit1:
          type: rabbit
          environment:
            spring:
              rabbitmq:
                host: ${rabbithostname:localhost}
                port: 5672
                username: guest
                password: guest
                
server:  
  port: ${PORT:2225}